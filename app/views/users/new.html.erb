<h2>Inscrire un nouvel utilisateur</h2>

<%= render 'global/errors', obj: @user %>
<%= form_with model: @user, class: "form-sign-up" do |f| %>
    <div class="form-group">
        <%= f.label :username, "Pseudo:" %>
        <%= f.text_field :username %>
    </div>
    <div class="form-group">
        <%= f.label :email, "Email:" %>
        <%= f.email_field :email %>
    </div>
    <div class="form-group">
        <%= f.label :password, "Mot de passe:" %>
        <%= f.password_field :password %>
    </div>
    <%= f.submit "Créer un nouvel utilisateur", class: "sumbit" %>
<% end %>